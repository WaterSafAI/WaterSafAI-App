import { screens } from './colors'

export const mainScreen = {
    flex: 1,
    backgroundColor: screens.background,
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'flex-start',
}